<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to "Edit this event", edit_user_event_path(@event) %>

  <%= button_to "Destroy this event", user_event_path(@event), method: :delete %>
</div>
